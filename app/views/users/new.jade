.page-header
  h1 New User

- form_for(user, {action: path_to.users(), method: 'POST', id: "user_form"}, function (form) {
  != partial('users/form.jade', {locals: {form: form, user: user}})
  .actions
    != form.submit('Create', {class: 'action primary'})
    != link_to('Cancel', path_to.users(), {class: 'action danger'})
- });
